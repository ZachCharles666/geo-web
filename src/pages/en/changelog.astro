---
import Layout from "../../layouts/Layout.astro";
import { SITE_URL, LEGAL_ENTITY_NAME } from "../../config/site";

const pageTitle = "Changelog | GEO-Max";
const pageDesc = "Product updates and improvements to GEO-Max.";
const canonical = "/en/changelog";
const hreflangs = [
  { href: "/en/changelog", hreflang: "en" },
  { href: "/zh/changelog", hreflang: "zh-CN" },
];
---

<Layout
  title={pageTitle}
  description={pageDesc}
  canonical={canonical}
  lang="en"
  hreflangs={hreflangs}
>
  <main class="max-w-5xl mx-auto px-4 sm:px-6 lg:px-8 py-12 sm:py-14">
    <h1 class="text-3xl sm:text-4xl font-semibold text-slate-900">Changelog</h1>
    <p class="mt-3 text-sm text-slate-500">Latest updates to the GEO tools platform.</p>

    <ul class="mt-8 space-y-6 text-sm sm:text-base text-slate-700">
      <li class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm">
        <p class="text-xs text-slate-500">2026-01-08</p>
        <p class="mt-2 font-semibold text-slate-900">Improved Alpha checkout reliability</p>
        <p class="mt-1">Added more robust error handling and clearer status messaging.</p>
      </li>
      <li class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm">
        <p class="text-xs text-slate-500">2025-12-22</p>
        <p class="mt-2 font-semibold text-slate-900">Rewrite prompt tuning pass</p>
        <p class="mt-1">Refined GEO-Rewrite prompts to improve factual structure and citations.</p>
      </li>
      <li class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm">
        <p class="text-xs text-slate-500">2025-12-10</p>
        <p class="mt-2 font-semibold text-slate-900">Score report clarity update</p>
        <p class="mt-1">Simplified the Score explanations and added confidence hints.</p>
      </li>
      <li class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm">
        <p class="text-xs text-slate-500">2025-11-28</p>
        <p class="mt-2 font-semibold text-slate-900">Expanded English/Chinese localization</p>
        <p class="mt-1">Aligned key UI labels and messages across EN and ZH pages.</p>
      </li>
      <li class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm">
        <p class="text-xs text-slate-500">2025-11-05</p>
        <p class="mt-2 font-semibold text-slate-900">Alpha access launch</p>
        <p class="mt-1">Introduced paid Alpha access tiers for GEO-Rewrite and GEO-Score.</p>
      </li>
    </ul>
  </main>

  <script type="application/ld+json">
    {JSON.stringify({
      "@context": "https://schema.org",
      "@type": "WebPage",
      "name": pageTitle,
      "url": new URL(canonical, SITE_URL).toString(),
      "inLanguage": "en",
      "publisher": {
        "@type": "Organization",
        "name": LEGAL_ENTITY_NAME,
        "url": SITE_URL
      }
    })}
  </script>
</Layout>
