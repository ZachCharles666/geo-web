---
import Layout from "../../layouts/Layout.astro";
import { LEGAL_ENTITY_NAME, SUPPORT_EMAIL, SITE_URL } from "../../config/site";

const pageTitle = "退款政策 | GEO-Max";
const pageDesc = "GEO-Max 订阅退款的条件与流程。";
const canonical = "/zh/refund";
const hreflangs = [
  { href: "/en/refund", hreflang: "en" },
  { href: "/zh/refund", hreflang: "zh-CN" },
];
const updatedAt = "2026-01-11";
---

<Layout
  title={pageTitle}
  description={pageDesc}
  canonical={canonical}
  lang="zh-CN"
  hreflangs={hreflangs}
>
  <main class="max-w-5xl mx-auto px-4 sm:px-6 lg:px-8 py-12 sm:py-14">
    <h1 class="text-3xl sm:text-4xl font-semibold text-slate-900">退款政策</h1>
    <p class="mt-3 text-sm text-slate-500">最后更新：{updatedAt}</p>

    <div class="mt-8 space-y-8 text-sm sm:text-base text-slate-700 leading-relaxed">
      <section class="space-y-3">
        <h2 class="text-xl font-semibold text-slate-900">退款政策</h2>
        <p>我们提供 14 天退款保障。</p>
        <p>
          如果你对购买不满意，可在首次付款日起 14 天内申请退款，我们将为你办理退款（不设附加条件）。
        </p>
      </section>

      <section class="space-y-3">
        <h2 class="text-xl font-semibold text-slate-900">如何申请退款</h2>
        <p>
          请发送邮件至
          <a class="text-indigo-600 hover:underline" href={`mailto:${SUPPORT_EMAIL}`}>{SUPPORT_EMAIL}</a>
          ，并提供：
        </p>
        <ul class="list-disc pl-5 space-y-1">
          <li>与账号绑定的邮箱</li>
          <li>订单信息（如收据或交易号，如有）</li>
        </ul>
        <p>我们通常会在 3-5 个工作日内回复。</p>
      </section>

      <section class="space-y-3">
        <h2 class="text-xl font-semibold text-slate-900">退款处理</h2>
        <p>
          付款由我们的计费服务提供商处理。退款一经批准，将原路退回至你的付款方式。到账时间可能因银行或发卡机构而异。
        </p>
      </section>
    </div>
  </main>

  <script type="application/ld+json">
    {JSON.stringify({
      "@context": "https://schema.org",
      "@type": "WebPage",
      "name": pageTitle,
      "url": new URL(canonical, SITE_URL).toString(),
      "inLanguage": "zh-CN",
      "publisher": {
        "@type": "Organization",
        "name": LEGAL_ENTITY_NAME,
        "url": SITE_URL
      }
    })}
  </script>
</Layout>
